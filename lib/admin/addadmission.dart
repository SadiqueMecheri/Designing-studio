import 'package:flutter/material.dart';

class addadmissions extends StatelessWidget {
  const addadmissions({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Add admissions"),);
  }
}
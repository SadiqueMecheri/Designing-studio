import 'package:flutter/material.dart';

class batchview extends StatelessWidget {
  const batchview({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("batchview"),);
  }
}